import { ColorIdentityParam } from './color-identity-param.model';

describe('ColorIdentityParam', () => {
  it('should create an instance', () => {
    expect(new ColorIdentityParam()).toBeTruthy();
  });
});
